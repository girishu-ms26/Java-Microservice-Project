spring:
  application:
    name: at0m-cloud-gateway

---
spring:
  profiles: dev
  cloud:
    config:
      uri:
        - http://localhost:8080/
---
spring:
  profiles: prod
  cloud:
    config:
      uri:
        - http://localhost:8080/
---